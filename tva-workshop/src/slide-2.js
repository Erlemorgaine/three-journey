import * as THREE from "three";

/**
 * Intro slide:
 * - At the start of CTM I was still a bit of a Noob
 * - Now I know way more thanks to Bruno Simon
 *
 * SCENE:
 * - 2 models of CTM
 * - Photof of Bruno with heart
 *
 * @param {*} scene
 * @param {*} textureLoader
 */
export const slide2 = (scene, textureLoader) => {};
