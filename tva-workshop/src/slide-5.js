import * as THREE from "three";

/**
 *
 * Lights / shadows
 * - You can use lights, but some are not performant
 * - You can use shadows, but also not performant
 * - Keep scene simple, with few objects and few lights
 *
 * SCENE:
 * Simple objects, choosing lights with dat.gui?
 *
 * @param {*} scene
 * @param {*} textureLoader
 */
export const slide5 = (scene, textureLoader) => {};
